---
import { SITE_BRAND } from '~/data/site';
---

<img
  src="/logo.jpg"
  alt={`${SITE_BRAND.name} — ${SITE_BRAND.tagline}`}
  class="h-16 w-auto max-w-full object-contain md:h-11"
  width="240"
  height="72"
  loading="eager"
  fetchpriority="high"
/>
